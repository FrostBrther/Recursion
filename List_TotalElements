#Pyhthon 3.0+ realisation for counting total elements in a list

def total_elements(list):
    sum = list.pop(0)
    total = 1
    if (len(list) == 0):
        return total
    else:
        return total + total_elements(list)
